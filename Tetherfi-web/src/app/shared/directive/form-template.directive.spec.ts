import { FormTemplateDirective } from './form-template.directive';

describe('FormTemplateDirective', () => {
    it('should create an instance', () => {
        const directive = new FormTemplateDirective();
        expect(directive).toBeTruthy();
    });
});
